# NOTE: PySpark also requires a compatible Java runtime.
# For this project, use Java 17 (newer Java versions can break Spark/Hadoop).

pyspark==4.0.2
pandas==2.3.3
pyarrow==21.0.0
numpy==2.0.2
pytest==8.4.2


